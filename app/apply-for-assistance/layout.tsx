// The page itself is a client component, so its metadata lives here.
export const metadata = {
  title: "Apply for Assistance | Tomchei Shabbos of Florida",
  description:
    "Apply confidentially for weekly Shabbos and Yom Tov food assistance from Tomchei Shabbos of Florida. All applications are handled with discretion and dignity.",
};

export default function ApplyForAssistanceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
