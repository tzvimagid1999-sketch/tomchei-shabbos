// The page itself is a client component, so its metadata lives here.
export const metadata = {
  title: "Yom Tov Campaign | Tomchei Shabbos of Florida",
  description:
    "Help a local family celebrate Yom Tov with dignity. Sponsor a Shabbos or Yom Tov food package for a family in South Florida — one-time or monthly.",
};

export default function RoshHashanahLayout({ children }: { children: React.ReactNode }) {
  return children;
}
